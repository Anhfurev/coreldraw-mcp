---
date: 2026-04-29
time: "00:01"
type: FIX
---

# Session 摘要 — 2026-04-29

## 完成了

- CorelDRAW X6 兼容性修复（连续迭代，最终 21/28 通过）：
  - 形状查找改为 duck-typing（`shape.Text` 而非 `Type == 3`）
  - `disconnect()` 移除 `Quit()` 调用，不再关闭 CorelDRAW
  - `reconnect_on_failure` 默认改为 `False`，防止逻辑失败触发重连级联
  - `export_dxf` 移除 `CreateStructExportOptions()` 依赖
  - `export_png/preview_png` 新增 ExportEx fallback 路径
  - `test_e2e.py` 修复步骤顺序：assign_to_layer 移至 convert_to_curves 之前
- 解决 _index.md 合并冲突（远端 feat-012/013/014/015 合并进来）

## 未完成

- PNG/DXF 导出仍有 3 个失败（"Python instance cannot be converted to COM object"），等待用户跑最新代码验证

## 遗留问题

- ExportEx 在 X6 下对 PNG/DXF 是否能静默导出尚未验证，等最新测试结果

## 下次建议从这里开始

运行 `python server/test_e2e.py`，如果 PNG/DXF 仍失败，查看 `server/tools/export.py` export_dxf / export_png 函数，考虑使用 `doc.Export(path, filter, 0)` 作为最终 fallback。
